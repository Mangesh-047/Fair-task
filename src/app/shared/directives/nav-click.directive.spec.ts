import { NavClickDirective } from './nav-click.directive';

describe('NavClickDirective', () => {
  it('should create an instance', () => {
    const directive = new NavClickDirective();
    expect(directive).toBeTruthy();
  });
});
